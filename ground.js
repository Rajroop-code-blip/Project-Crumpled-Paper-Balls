class Ground
{
    display() {
    groundObj.display;

 groundObj=new ground(width/2,670,width,20);
  leftSide = new ground(1100,600,20,120);
  rightSide = new ground(110,600,20,120);
}
 }

function keyPressed() {
    if(keyCode === UP_ARROW){
    
    }
}

